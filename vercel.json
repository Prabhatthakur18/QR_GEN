{
    "buildCommand": "npm install",
    "outputDirectory": "public",
    "rewrites": [
        {
            "source": "/verify/:path*",
            "destination": "/index.html"
        }
    ],
    "functions": {
        "api/**/*.js": {
            "memory": 128,
            "maxDuration": 10
        }
    }
}